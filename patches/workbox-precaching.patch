diff --git a/PrecacheController.js b/PrecacheController.js
index e00975e3762dc6382c39bebee04a89a651aae3d0..5997eed4592f930037996b7b0767dfffa4d19952 100644
--- a/PrecacheController.js
+++ b/PrecacheController.js
@@ -17,6 +17,17 @@ import { printCleanupDetails } from './utils/printCleanupDetails.js';
 import { printInstallDetails } from './utils/printInstallDetails.js';
 import { PrecacheStrategy } from './PrecacheStrategy.js';
 import './_version.js';
+
+const runWithConcurrency = async (items, limit, handler) => {
+    if (limit <= 0) {
+        throw new Error('limit must be greater than 0');
+    }
+    for (let i = 0; i < items.length; i += limit) {
+        const batch = items.slice(i, i + limit);
+        // Await each batch sequentially to keep the total concurrency bounded.
+        await Promise.all(batch.map((item) => handler(item)));
+    }
+};
 /**
  * Performs efficient precaching of assets.
  *
@@ -150,9 +161,9 @@ class PrecacheController {
         return waitUntil(event, async () => {
             const installReportPlugin = new PrecacheInstallReportPlugin();
             this.strategy.plugins.push(installReportPlugin);
-            // Cache entries one at a time.
+            // Cache entries with controlled concurrency to avoid overwhelming the network.
             // See https://github.com/GoogleChrome/workbox/issues/2528
-            for (const [url, cacheKey] of this._urlsToCacheKeys) {
+            await runWithConcurrency([...this._urlsToCacheKeys.entries()], 10, async ([url, cacheKey]) => {
                 const integrity = this._cacheKeysToIntegrities.get(cacheKey);
                 const cacheMode = this._urlsToCacheModes.get(url);
                 const request = new Request(url, {
@@ -165,7 +176,7 @@ class PrecacheController {
                     request,
                     event,
                 }));
-            }
+            });
             const { updatedURLs, notUpdatedURLs } = installReportPlugin;
             if (process.env.NODE_ENV !== 'production') {
                 printInstallDetails(updatedURLs, notUpdatedURLs);
diff --git a/src/PrecacheController.ts b/src/PrecacheController.ts
index bb7f97e59926dc60b528a8f5fbd61f21a42414f9..6d1d73b979ac9498a9c31f60338489825f676363 100644
--- a/src/PrecacheController.ts
+++ b/src/PrecacheController.ts
@@ -213,6 +213,7 @@ class PrecacheController {
           integrity,
           cache: cacheMode,
           credentials: 'same-origin',
+          priority: 'low'
         });
 
         await Promise.all(
