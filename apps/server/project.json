{
  "name": "template-server",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/server",
  "projectType": "application",
  "targets": {
    "app-build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/apps/server"],
      "options": {
        "command": "xdev build --cwd apps/server"
      }
    },
    "app-deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "xdev deploy --cwd apps/server"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "xdev serve --cwd apps/server"
      }
    },
    "preview": {
      "dependsOn": ["app-build"],
      "executor": "nx:run-commands",
      "options": {
        "command": "xdev --tsconfig=apps/server/tsconfig.app.json preview --cwd apps/server"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/node_modules/.vitest/apps/server", "{workspaceRoot}/coverage/apps/server"],
      "options": {
        "command": "xdev test --project template-server"
      }
    },
    "madge": {
      "executor": "nx:run-commands",
      "options": {
        "command": "madge --ts-config ./tsconfig.app.json --circular --no-color --no-spinner --extensions ts,tsx .",
        "cwd": "apps/server"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsgo -p apps/server/tsconfig.check.json"
      }
    },
    "db": {
      "executor": "nx:run-commands",
      "defaultConfiguration": "seed",
      "configurations": {
        "seed": {
          "command": "xdev db seed --cwd apps/server"
        },
        "push": {
          "command": "xdev db push --cwd apps/server"
        },
        "dump": {
          "command": "xdev db dump --cwd apps/server"
        },
        "execute": {
          "command": "xdev db execute --cwd apps/server"
        },
        "dev": {
          "command": "xdev db migrate dev --cwd apps/server"
        },
        "reset": {
          "command": "xdev db migrate reset --cwd apps/server"
        },
        "deploy": {
          "command": "xdev db migrate deploy --cwd apps/server"
        },
        "resolve": {
          "command": "xdev db migrate resolve --cwd apps/server"
        }
      }
    }
  }
}
