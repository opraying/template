# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://github.com/evilmartians/lefthook/blob/master/docs/configuration.md
#
pre-push:
  commands:
    madge:
      run: CI=true ./scripts/run-nx-affected.sh 'nx affected --target=madge'
    typecheck:
      run: CI=true ./scripts/run-nx-affected.sh 'nx affected --target=typecheck'

pre-commit:
  commands:
    env-check:
      glob: '*.env*'
      run: dotenvx ext precommit
    lint:
      glob: '*.{js,ts,mjs,jsx,tsx,json,jsonc}'
      run: oxlint
    format:
      glob: '*.{js,ts,mjs,jsx,tsx,json,jsonc}'
      run: oxfmt --check && git update-index --again
