{
  "name": "pkgs-server-testing",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/server-testing/src",
  "projectType": "library",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/node_modules/.vitest/packages/server-testing",
        "{workspaceRoot}/coverage/packages/server-testing"
      ],
      "options": {
        "command": "pnpm xdev test --project pkgs-server-testing"
      }
    },
    "madge": {
      "executor": "nx:run-commands",
      "options": {
        "command": "madge --ts-config ./tsconfig.lib.json --circular --no-color --no-spinner --extensions ts,tsx ./src",
        "cwd": "packages/server-testing"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsgo -p packages/server-testing/tsconfig.check.json"
      }
    }
  },
  "tags": []
}
