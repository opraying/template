{
  "name": "pkgs-local-first",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/local-first/src",
  "projectType": "library",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/node_modules/.vitest/packages/local-first",
        "{workspaceRoot}/coverage/packages/local-first"
      ],
      "options": {
        "command": "pnpm xdev test --project pkgs-local-first"
      }
    },
    "madge": {
      "executor": "nx:run-commands",
      "options": {
        "command": "madge --ts-config ./tsconfig.lib.json --circular --no-color --no-spinner --extensions ts,tsx ./src",
        "cwd": "packages/local-first"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsgo -p packages/local-first/tsconfig.check.json"
      }
    }
  },
  "tags": []
}
