{
  "name": "pkgs-do-proxy",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/sql-do-proxy/src",
  "projectType": "library",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/node_modules/.vitest/packages/sql-do-proxy",
        "{workspaceRoot}/coverage/packages/sql-do-proxy"
      ],
      "options": {
        "command": "xdev test --project pkgs-sql-do-proxy"
      }
    },
    "madge": {
      "executor": "nx:run-commands",
      "options": {
        "command": "madge --ts-config ./tsconfig.lib.json --circular --no-color --no-spinner --extensions ts,tsx ./src",
        "cwd": "packages/sql-do-proxy"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsgo -p packages/sql-do-proxy/tsconfig.check.json"
      }
    }
  },
  "tags": []
}
